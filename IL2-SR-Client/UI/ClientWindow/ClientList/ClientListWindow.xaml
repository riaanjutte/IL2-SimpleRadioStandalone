<Window x:Class="Ciribob.IL2.SimpleRadio.Standalone.Client.UI.ClientWindow.ClientList.ClientListWindow"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      Title="Connected Clients"
                      MinWidth="300"
                      MinHeight="390"
                      Width="300"
                      Height="390"
                      AllowsTransparency="True"
                      ResizeMode="CanResizeWithGrip"
                      WindowStyle="None"
                      Background="Transparent"
                      Foreground="{DynamicResource MilTextPrimaryBrush}"
                      FontSize="14"
                      xmlns:clients="clr-namespace:Ciribob.IL2.SimpleRadio.Standalone.Client.UI.ClientWindow.ClientList"
                     >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/IL2-SR-ClientRadio;component/Themes/MilitaryControls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <GroupBox Header="Roster" Margin="6,2,6,6" MouseLeftButtonDown="ClientListWindow_MouseLeftButtonDown">
        <ListBox Name="ClientList"
                 HorizontalContentAlignment="Stretch"
                 Padding="5"
                 Background="{DynamicResource MilWellBrush}"
                 Foreground="{DynamicResource MilTextPrimaryBrush}"
                 BorderBrush="{DynamicResource MilBorderBrush}"
                 BorderThickness="1"
                 FontFamily="{StaticResource MilFontMono}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="4,2" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{DynamicResource MilTextPrimaryBrush}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource MilRowAltBrush}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource MilActionBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2,4,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}"
                                   Grid.Column="0"
                                   Margin="0,0,8,0"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Channel}"
                                   Grid.Column="1"
                                   Margin="0,0,10,0"
                                   VerticalAlignment="Center" />

                        <!-- coalition jewel lamp -->
                        <Grid Width="14"
                              Height="14"
                              Grid.Column="2"
                              Margin="0,0,2,0"
                              VerticalAlignment="Center">
                            <Ellipse Fill="{DynamicResource MilLampBezelBrush}" />
                            <Ellipse Margin="2" Fill="{Binding ClientCoalitionColour}" />
                            <Ellipse Margin="2" Fill="{DynamicResource MilLampGlassBrush}" />
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
    </GroupBox>

</Window>
