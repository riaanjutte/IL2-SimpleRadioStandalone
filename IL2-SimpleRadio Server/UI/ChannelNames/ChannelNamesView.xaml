<UserControl x:Class="Ciribob.IL2.SimpleRadio.Standalone.Server.UI.ChannelNames.ChannelNamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="390"
             Height="590">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   Text="Names are optional. Blank channels use their standard CHN number."
                   TextWrapping="Wrap" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="4"
                       FontWeight="Bold"
                       Text="Channel" />
            <TextBlock Grid.Row="0"
                       Grid.Column="1"
                       Margin="4"
                       FontWeight="Bold"
                       Text="Name" />

            <ScrollViewer Grid.Row="1"
                          Grid.Column="0"
                          Grid.ColumnSpan="2"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Channels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Margin="4,3"
                                           VerticalAlignment="Center"
                                           Text="{Binding Channel}" />
                                <TextBox Grid.Column="1"
                                         Height="24"
                                         Margin="4,2"
                                         MaxLength="32"
                                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="2"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button x:Name="Save"
                    Width="80"
                    Height="26"
                    Margin="0,0,8,0"
                    Content="Save" />
            <Button x:Name="Cancel"
                    Width="80"
                    Height="26"
                    Content="Cancel" />
        </StackPanel>
    </Grid>
</UserControl>
